<%= current_user.name %>
<%= link_to "Log out", logout_path, method: :delete %>

<% @restaurants.each do |r| %>
  <h3><%= r.name %></h3>
  <p>Votes: <%= r.plusminus %></p>

  <%= link_to 'Upvote', upvote_path(restaurant_id: r.id), method: :post %>
<%end%>


